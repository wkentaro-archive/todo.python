sudo: false

language: python

python:
- '2.7'
- '3.3'

before_install:
- pip install -U pip setuptools
- pip install hacking pytest

install:
- pip install .

before_script:
- flake8 .

script:
- pytest -vs .

branches:
  only:
    - master
